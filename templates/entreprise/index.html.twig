{% extends 'base.html.twig' %}

{% block title %}Hello EntrepriseController!{% endblock %}

{% block body %}
    <h1>Liste des entreprises</h1>

    {# ceci est une boucle foreach  (for row in array) #}
    {% for entreprise in entreprises %}
        
        <a href="{{ path('show_entreprise', {'id':entreprise.id}) }}">{{entreprise.raisonSociale }}</a><br> {#  il représente un echo en PHP#}
    {% endfor %}
{% endblock %}
